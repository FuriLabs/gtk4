[DEFAULT]
pristine-tar = True
debian-branch = debian/trixie
upstream-branch = upstream/4.10.x
upstream-vcs-tag = %(version)s

[buildpackage]
sign-tags = True

[import-orig]
postimport = dch -v%(version)s New upstream release; git add debian/changelog; debcommit

[pq]
patch-numbers = False
